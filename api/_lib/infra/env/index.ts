import {
  APP_VERSION,
  APP_ENV_ENCRYPTION_DETAILS,
} from './app'

import {
  REACT_APP_JWT_SECRET,
} from './react_app_jwt'

import {
  MONGODB_URL,
  ROOMS_DB_NAME,
} from './mongo'

const ENV = {
  APP_VERSION,
  APP_ENV_ENCRYPTION_DETAILS,

  REACT_APP_JWT_SECRET,

  MONGODB_URL,
  ROOMS_DB_NAME,
}

export {
  ENV,
}
