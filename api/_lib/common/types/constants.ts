import { IProfileRole, IHttpStatus, IHttpStatusCode } from '../types'

interface IConstants {
  PROFILE_ROLE: IProfileRole
  HTTP_STATUS_CODE: IHttpStatusCode
  HTTP_STATUS: IHttpStatus
}

export { IConstants }
