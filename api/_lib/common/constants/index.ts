import {
  IConstants,
} from '../../common/types'

import {
  PROFILE_ROLE,
} from './profile_role'

import {
  HTTP_STATUS_CODE,
  HTTP_STATUS,
} from './http_status'

const CONSTANTS: IConstants = {
  PROFILE_ROLE,

  HTTP_STATUS_CODE,
  HTTP_STATUS,
}

export {
  CONSTANTS,
}
